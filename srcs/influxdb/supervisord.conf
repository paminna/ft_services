[supervisord]
nodaemon=true
# [program:telegraf]
# command= sh -c "telegraf && kill -s SIGTERM $(cat supervisord.pid)"
# [program:grafana]
# command= sh -c "influxd run -config /etc/telegraf.conf && kill -s SIGTERM $(cat supervisord.pid)"
